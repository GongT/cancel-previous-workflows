name: 'Cancel Previous Workflow Runs'
description: 'A Github action that cancels all previous workflows for older commits in its branch (workflow specific)'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'archive'  
  color: 'blue'
