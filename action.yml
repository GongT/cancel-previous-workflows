name: 'Cancel Previous Runs Of Current Workflow'
description: 'A Github action that cancels all previous runs for older commits in its branch (workflow specific)'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: 'archive'  
  color: 'blue'
